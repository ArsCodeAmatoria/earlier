import { Action, SceneHeading, Transition } from "./primitives";

/** Scene 22 — Final image (clearing coda) (screenplay body) */

export function Scene22FinalImage() {
  return (
    <article>
      <Action>SCENE 22 — FINAL IMAGE</Action>

      <SceneHeading>EXT. FOREST CLEARING — MORNING</SceneHeading>

      <Action>Soft daylight.</Action>
      <Action>Still. Quiet.</Action>
      <Action>The clearing is empty.</Action>
      <Action>No tents.</Action>
      <Action>No gear.</Action>
      <Action>No fire.</Action>
      <Action>The ground shows faint disturbance—</Action>
      <Action>subtle, uneven patches—</Action>
      <Action>like something was there.</Action>
      <Action>Near the center—</Action>
      <Action>a small cluster of mushrooms.</Action>
      <Action>Nothing moves.</Action>
      <Action>The trees stand close around the clearing.</Action>
      <Action>No wind.</Action>
      <Action>No sound.</Action>
      <Action>The space feels complete—</Action>
      <Action>and incomplete at the same time.</Action>
      <Action>A portion of the clearing—</Action>
      <Action>just off-center—</Action>
      <Action>feels like it should be occupied.</Action>
      <Action>But it isn’t.</Action>
      <Action>The frame holds.</Action>
      <Action>No movement.</Action>
      <Action>No indication of time passing.</Action>
      <Action>Just the clearing.</Action>
      <Action>Exactly as it is.</Action>
      <Action>The image lingers—</Action>
      <Action>longer than expected—</Action>
      <Action>long enough to notice what isn’t there—</Action>
      <Action>but not long enough to confirm it.</Action>
      <Action>Stillness.</Action>

      <Transition>CUT TO BLACK.</Transition>
    </article>
  );
}
