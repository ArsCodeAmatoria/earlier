/** Official logline — used on the home hero and default SEO description. */
export const LOGLINE =
  "Three friends take a camping trip to stage an intervention for one of them—but after accidentally ingesting wild mushrooms, the plan collapses, one of them disappears, and the remaining two can’t agree on what actually happened.";

/** Characters section intro (above bios). */
export const CHARACTERS_SECTION_INTRO =
  "Leanne, Maya, and Nora are written as pressure points, not types—each voice distinct, each line doing two jobs. The script leans on subtext and repetition so paranoia lands without exposition dumps. These bios are a map for cast and collaborators reading like sides.";

/** Sets section intro (above principles / locations). */
export const SETS_SECTION_INTRO =
  "The story moves from road to trail to clearing to night fire to woods that won’t stay mapped—and every location is doing thriller work on the page. What follows is how we keep the world tactile and believable while the draft quietly unthreads space and time.";

/** Contact section intro. */
export const CONTACT_INTRO =
  "Screeners, PDFs, and festival packets on request. If you’re programming, packaging, or casting: we’re glad to talk tone, structure, and why this one earns its final image.";

/** Primary contact email (home contact + header dialog). */
export const CONTACT_EMAIL = "kojinfox@gmail.com";

/** Festival submission PDF (replace file in public/PDFs after full export from master draft). */
export const FESTIVAL_SCREENPLAY_PDF_HREF = "/PDFs/earlier-festival-draft.pdf";
export const FESTIVAL_SCREENPLAY_PDF_FILENAME = "earlier-festival-draft.pdf";

/** Principal filming locations */
export const FILMING_LOCATIONS_PLACES = "Hope, BC · Silver Lake";
export const FILMING_LOCATIONS_LINE = `Filmed in ${FILMING_LOCATIONS_PLACES}`;
